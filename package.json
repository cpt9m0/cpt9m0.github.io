{
  "private": true,
  "scripts": {
    "build:js": "esbuild assets/js/site.js --bundle --minify --outfile=assets/js/site.min.js",
    "build": "npm run build:js && bundle exec jekyll build",
    "serve": "npm run build:js && bundle exec jekyll serve --livereload",
    "start": "npm run serve"
  },
  "dependencies": {
    "bootstrap": "5.3.3"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
